from setuptools import setup, find_packages

setup(
    name='pmmtracing',
    #version=__version__,
    description='',
    url='https://github.com/mapmanager/Tracing',
    author='',
    author_email='',
    license='GNU GPLv3',
    #packages = find_packages(),
    packages=find_packages(include=['pmmtracing']),
    install_requires=[
    ]
)
